# 后续可扩展方向

- **学习类型分类**：如「编程」「阅读」等标签，分别统计与展示。
- **统计图表**：按日/周的学习时长、娱乐使用趋势（可用 ECharts 等）。
- **PWA**：离线计时、安装到桌面、基础离线缓存。
- **规则扩展**：如「仅周末可兑换娱乐」等，可在前端或 Supabase Edge Function 中实现。
- **多设备同步**：已由 Supabase 默认支持（同一账号登录即可）。
